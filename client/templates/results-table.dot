<h3>{{=it.filename}} (was parsed by {{=it.parsingType}})</h3>

<p>Start time: {{=(new Date(it.startTime)).toLocaleString()}}</p>
<p>Finish time: {{=(new Date(it.finishTime)).toLocaleString()}}</p>
<p>Total time: {{=it.finishTime - it.startTime}} milliseconds</p>

<table class="fn-results">
    <thead>
    <tr>
        <th>Column name</th>
        <th>Filled cells (in percent)</th>
        <th>Unique cells count</th>
        <th>Column type</th>
    </tr>
    </thead>
    <tbody>
    {{~it.columnData:column}}
        <tr>
            <td>{{=column.title}}</td>
            <td>{{=Math.round(column.filledCellsCount / column.cellsCount * 100)}}</td>
            <td>{{=column.uniqueCellsCount}}</td>
            <td>{{=column.type}}</td>
        </tr>
    {{~}}
    </tbody>
</table>

<div>
    <h5>Common info:</h5>
    <span>Count of rows: {{=it.columnData[0].cellsCount}}</span>
    <span>Count of columns: {{=it.columnData.length}}</span>
</div>